@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


@model List<ClinicQueue.Models.User>
@{
    ViewData["Title"] = "Users";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div class="d-flex align-items-center">
        <i class="bi bi-people-fill text-primary fs-3 me-2"></i>
        <h4 class="fw-bold mb-0">All Users</h4>
    </div>
    <a href="/Admin/CreateUser" class="btn btn-primary btn-sm">
        <i class="bi bi-person-plus me-1"></i>Create User
    </a>
</div>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white d-flex justify-content-between align-items-center">
        <span class="text-muted small">Total: <strong>@Model.Count</strong></span>
        <input id="search" class="form-control form-control-sm w-25"
               placeholder="Search..." onkeyup="filterTable()" />
    </div>
    <div class="card-body p-0">
        @if (!Model.Any())
        {
            <p class="text-center text-muted py-4">No users found.</p>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0" id="usersTable">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Role</th>
                            <th>Phone</th>
                            <th>Created</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.Count; i++)
                        {
                            var u = Model[i];
                            var badge = u.Role.ToLower() switch
                            {
                                "admin" => "bg-danger",
                                "doctor" => "bg-primary",
                                "patient" => "bg-success",
                                "receptionist" => "bg-warning text-dark",
                                _ => "bg-secondary"
                            };
                            <tr>
                                <td class="text-muted">@(i + 1)</td>
                                <td>
                                    <div class="d-flex align-items-center gap-2">
                                        <div class="avatar-sm bg-primary text-white rounded-circle d-flex align-items-center justify-content-center">
                                            @u.Name[0].ToString().ToUpper()
                                        </div>
                                        <span class="fw-semibold">@u.Name</span>
                                    </div>
                                </td>
                                <td class="text-muted">@u.Email</td>
                                <td>
                                    <span class="badge @badge text-capitalize">@u.Role</span>
                                </td>
                                <td class="text-muted">@(u.Phone ?? "—")</td>
                                <td class="text-muted small">
                                    @(u.CreatedAt?.ToString("dd MMM yyyy") ?? "—")
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        function filterTable() {
            var val = document.getElementById('search').value.toLowerCase();
            document.querySelectorAll('#usersTable tbody tr').forEach(function(row) {
                row.style.display = row.textContent.toLowerCase().includes(val) ? '' : 'none';
            });
        }
    </script>
}